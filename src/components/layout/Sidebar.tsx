
import { Sidebar as SidebarComponent } from "./sidebar/Sidebar";

// Export the Sidebar component from the new location
// This ensures backward compatibility with existing imports
export const Sidebar = SidebarComponent;
